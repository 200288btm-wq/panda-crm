meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    dist: /

run:
  command: npx serve -s /app -l 80
  containerPort: 80
