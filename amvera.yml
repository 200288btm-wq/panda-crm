meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    dist: /

run:
  scriptName: preview
  scriptArguments: --port 80 --host
  containerPort: 80
