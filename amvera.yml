meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build && npm install -g serve
  artifacts:
    dist: /

run:
  command: serve -s /app -l 80
  containerPort: 80
