meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    dist: /

run:
  command: npx --yes serve@14 -s /app -l 80
  containerPort: 80
