meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands:
    - npm run build
  artifacts:
    dist: /

run:
  command: npm run preview -- --port 80 --host
  containerPort: 80
